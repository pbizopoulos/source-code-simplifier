Source Code Simplifier for Python:
- https://pbizopoulos.github.io/source-code-simplifier

Requirements:
- POSIX compliant shell (ash, bash, dash, ksh, posh, yash or zsh)
- Git (Any version)
- Make (bmake or GNU Make)
- Python (>=3.9)

Preparation:
git clone https://github.com/pbizopoulos/source-code-simplifier
cd source-code-simplifier/

Usage:
make -f Makefile.server # Setup local server.
make			# Run tests.
make clean 		# Remove dependent directories.
make help		# Show all commands.
